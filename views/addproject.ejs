<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmation</title>
</head>
<body>
    <h1>Add Project Experience</h1>
    <form method="POST" action="/submitProject">
        <br>
        <p>Enter Project Name:</p>
        <input class="input-field" type="text" name="project" value="<%= data.project %>">
        <br>
        <p>Enter time worked (Ex: 2020-2023, 2020-present):</p>
        <input class="input-field" type="text" name="timeWorked" value="<%= data.timeWorked %>">
        <br>
        <p>Enter Skills Aquired:</p>
        <input class="input-field" type="text" name="skills" value="<%= data.skills %>">
        <br>
        <p>Project Description:</p>
        <textarea class="input-field" name="desc"><%= data.desc %></textarea>
        <br>
        <button class="submit-button" type="submit">Submit</button>
        <% if (errors) { %>
            <% errors.forEach((error) => { %>
                <p class="errors"><%= error %></p>
        <% }); %>
        <% } %>
    </form>
</body>
</html>